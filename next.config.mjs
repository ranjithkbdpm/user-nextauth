/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'avatars.githubusercontent.com'], 
    } // Add any other domains to permit loading images from external sources 
};

export default nextConfig;
